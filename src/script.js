document.addEventListener('DOMContentLoaded', function() {
  /*const body = document.getElementsByTagName('body');
  body.forEach(function(elem, i) {
    const listElem = elem.querySelector('.history__transaction');
    listElem.forEach(function(elem, i) {
      elem.addEventListener('click', function(e) {
        const showElem = elem.querySelector('history__show');
        const hiddenElem = elem.querySelector('history__hide');
        showElem.style.display = 'none';
        hiddenElem.style.display = 'block';
      });
    });
  });*/

  const body = document.getElementsByTagName('body');
  body.addEventListener();
});
